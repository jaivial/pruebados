/** @type {import('next').NextConfig} */
const nextConfig = {
  outDir: 'out', // or the desired output directory name
  images: {
    unoptimized: true,
  },
  // Add other configurations if needed
};

module.exports = nextConfig;
